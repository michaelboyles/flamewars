{
    "$id": "TODO, after pushing",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Add comment response",
    "description": "A response to a request to add a new comment",
    "type": "object",
    "properties": {
        "success": {
            "type": "boolean"
        },
        "comment": {
            "$ref": "comment.schema.json"
        },
        "rejectionReason": {
            "type": "string"
        }
    },
    "required": ["success", "comment"],
    "additionalProperties": false
}