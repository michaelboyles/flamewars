{
    "$id": "TODO, after pushing",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Get comment count response",
    "type": "object",
    "definitions": {
        "commentCount": {
            "type": "object",
            "properties": {
                "url": {
                    "type": "string"
                },
                "count": {
                    "type": "integer",
                    "minimum": 0
                }
            },
            "required": ["url", "count"]
        }
    },  
    "properties": {
        "counts": {
            "type": "boolean",
            "items": {
                "$ref": "#/definitions/commentCount"
            }
        },
        "success": {
            "type": "boolean"
        }
    },
    "required": ["counts", "success"]
}